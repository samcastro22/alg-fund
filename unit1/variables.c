#include <stdio.h>

//Datatype: integers(int), flotantes(float), cadenas de texto (char*) (string), caracteres (char)
//cajitas que almacenan info, para usarlas depues en codigo:
//Global Variables//
int (enteros) edad; //enteros solo almacenan datos numéricos 
float estatura= 1.70; //almacenan más espacio en bites que int
char* name = "Samantha Castro";
char sexo = 'F';

int main(){

    return 0;
}